import tkinter as tk
window = tk.Tk()
window.title("Chương trình GUI Python")
label = tk.Label(window, text="Xin chào, đây là một nhãn (label)!",font=("Arial Bold",50))
label.pack()
window.mainloop()
